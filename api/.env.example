# You can keep all env values in project root `.env`.
# This file is optional fallback for API-only runs.
PORT=4000
NODE_ENV=development
CLIENT_URL=http://localhost:5173
DATABASE_URL=postgresql://USER:PASSWORD@HOST.neon.tech/neednest?sslmode=require
JWT_SECRET=replace_with_strong_secret
JWT_EXPIRES_IN=7d
