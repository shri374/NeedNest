{
  "name": "neednest",
  "private": true,
  "scripts": {
    "dev": "concurrently -k -n API,WEB -c green,blue \"npm --prefix api run dev\" \"npm --prefix web run dev\"",
    "setup": "npm --prefix api install && npm --prefix web install",
    "build": "npm --prefix web run build",
    "start": "npm --prefix api run start",
    "prisma:generate": "npm --prefix api run prisma:generate",
    "prisma:migrate": "npm --prefix api run prisma:migrate",
    "db:seed": "npm --prefix api run db:seed"
  },
  "devDependencies": {
    "concurrently": "^9.2.1"
  }
}
